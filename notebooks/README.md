# Notebooks

The notebooks are grouped by use:

- `training`: main patient-independent training notebooks
- `leaky`: random-window baseline notebooks
- `analysis`: patient-specific, SHAP, band-ablation, and statistics notebooks

Outputs were cleared before committing. Re-run cells from the repository root, or keep the bootstrap cell that adds `src` to `sys.path`.
